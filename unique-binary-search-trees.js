// Generated by CoffeeScript 1.9.2
var numTrees;

numTrees = function(n) {
  var count, cur, i, j, left, right;
  count = new Array(n + 2);
  count[0] = 1;
  count[1] = 1;
  i = 2;
  while (i <= n) {
    j = 0;
    while (j < i) {
      left = count[j];
      right = count[i - j - 1];
      cur = count[i] ? count[i] : 0;
      count[i] = cur + left * right;
      j++;
    }
  }
  return count[n];
};
